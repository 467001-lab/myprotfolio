# พัฒนาจากต้นแบบ โดย: เปลี่ยนเป็นระบบรับค่าจากผู้ใช้ และคำนวณได้ทุกขนาด

print("--- โปรแกรมคำนวณพื้นที่สี่เหลี่ยมมุมฉาก (เวอร์ชันอัปเกรด) ---")

# รับค่าจากผู้ใช้งาน และแปลงเป็นตัวเลขทศนิยม (float) เพื่อให้ใส่จุดทศนิยมได้
กว้าง = float(input("กรุณาใส่ความกว้าง (เมตร): "))
ยาว = float(input("กรุณาใส่ความยาว (เมตร): "))

# ตรวจสอบเงื่อนไขว่าข้อมูลถูกต้องหรือไม่ (ห้ามเป็นศูนย์หรือติดลบ)
if กว้าง <= 0 or ยาว <= 0:
    print("❌ ผิดพลาด: ความกว้างและความยาวต้องมากกว่า 0!")
else:
    พื้นที่ = กว้าง * ยาว
    print("----------------------------------------")
    print(f"📐 สี่เหลี่ยมกว้าง {กว้าง} ม. และ ยาว {ยาว} ม.")
    print(f"✅ พื้นที่ทั้งหมดคือ: {พื้นที่:.2f} ตารางเมตร")
    print("----------------------------------------")
